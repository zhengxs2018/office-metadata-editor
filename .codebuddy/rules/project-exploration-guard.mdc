# 项目探索守卫规则

> 适用：主会话（组长）+ 所有子 agent。本规则约束代码探索行为，防止上下文爆炸与子 agent 卡死。

## 一、探索优先级：目录结构优先，文件名推断先行

1. **先用 `list_dir` / `search_file` 摸清目录骨架**，不要拿到任务就直接 `read_file`。
2. **根据文件名猜测内容角色**：`*-page.tsx` = 页面入口，`*-sidebar.tsx` = 侧边栏，`*-toolbar.tsx` = 工具栏，
   `*-dialog.tsx` = 弹窗，`*.css` = 样式。先建心智模型，再按需深入。
3. **`src/components/ui/` 目录默认跳过**：该目录为 shadcn/ui 自动生成的原子组件，不参与业务探索。
   仅当需要修复/修改 UI 组件本身时才进入该目录。

## 二、读取限制：首次只读前 20 行

1. **探索阶段**：对陌生文件首次 `read_file` 限定 `limit=20`，仅读取文件头部（import、组件签名、主要结构）。
2. **扩展条件**：仅在以下情况允许读更多行：
   - 需要修复该文件的 bug
   - 需要修改该文件的具体实现
   - 20 行不足以确定该文件是否为目标文件（此时可放宽到 50 行）
3. **禁止全量读取**：不得在探索阶段 `read_file` 不带 `limit` 参数读取整个文件。
   确认目标文件并进入修改阶段后，方可全量读取。

## 三、首次探索域限制（子 agent 硬边界）

1. 子 agent 首次探索**仅允许访问以下范围**：
   - `src/` 目录（跳过 `src/components/ui/`）
   - `src-tauri/src/` 目录
   - 根目录配置文件（`package.json`、`tsconfig.json`、`vite.config.ts`、`components.json`、`design.md` 等）
2. **禁止递归进入 `node_modules/`、`dist/`、`.git/`、`src-tauri/target/`**。
3. 超出以上范围的需求 → `escalate` 给主会话，由主会话决定是否扩大探索域。

## 四、主会话先行收缩范围（防子 agent 爆炸）

1. **主会话在派探索任务前，必须先自己用 IDE 工具（`list_dir` / `search_file` / `search_content` 仅 `files_with_matches`）做一次粗探**，
   拿到文件清单与数量级。
2. 根据粗探结果决策：
   - 文件 ≤ 10 个 → 主会话自己读即可，不派子 agent
   - 文件 10–30 个 → 拆为 2–3 个同步 `task`，按目录/模块切分
   - 文件 > 30 个 → 先上报用户确认范围，或按优先级分批
3. 派子 agent 时，**prompt 中必须明确**：
   - 允许访问的具体文件列表或子目录（绝对路径）
   - 每个文件只读前 20 行的限制
   - 输出要求：≤5 行摘要 + 文件角色映射表，不贴代码

## 五、探索结果落长期记忆

1. 每次项目探索完成后，**必须**将关键发现写入 `.codebuddy/memory/MEMORY.md`（更新对应章节）。
2. 记录内容至少包含：
   - 项目目录结构概要（主要模块与职责）
   - 关键文件角色映射（哪个文件负责什么）
   - 技术栈与依赖关系（框架、库、构建工具）
   - 发现的潜在问题或技术债务点
3. 后续会话在开始工作前，**必须先读 MEMORY.md** 复用已有探索成果，避免重复探索。

## 六、子 agent 探索 SOP（标准流程）

```
1. list_dir <目标目录>              # 先看目录骨架
2. search_file <pattern> <目录>     # 按文件名模式定位
3. read_file <file> limit=20        # 前 20 行验证猜测
4. 汇总 ≤5 行摘要 → DONE             # 不回传代码内容
```

违反上述流程（如跳过 list_dir 直接 read_file 全文）视为不合格执行。
