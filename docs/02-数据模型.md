# 数据模型

所有跨 IPC 的结构均以 `serde(rename_all = "camelCase")` 序列化，Rust 侧 `snake_case` 字段在前端呈现为 `camelCase`。字段名两侧严格一致，禁止漂移。

## 比对引擎数据模型

### 输入模型

```mermaid
classDiagram
    class CompareFileInput {
        +String id
        +String companyId
        +String companyName
        +String fileName
        +String creator
        +String lastModifiedBy
        +String appCompany
        +String manager
        +String template
        +String application
        +String appVersion
        +String created
        +String modified
        +String revision
        +String title
        +String subject
        +String keywords
        +String description
        +String category
        +String contentStatus
        +String version
        +String language
        +String totalTime
        +Vec~String~ annotationAuthors
        +Vec~String~ revisionAuthors
        +Vec~String~ xmpCreators
        +bool hasHiddenMarkers
    }

    class CompareOptions {
        +Option~i64~ closeTimestampMinutes
        +Option~f64~ fuzzyMatchFloor
        +Option~Map~ ruleOverrides
        +closeTimestampMinutes() i64
        +fuzzyMatchFloor() f64
        +ruleEnabled(ruleId) bool
        +ruleLevelOverride(ruleId) Option~RiskLevel~
    }

    class RuleOverride {
        +Option~bool~ enabled
        +Option~String~ level
    }

    CompareOptions "1" o-- "0..*" RuleOverride : ruleOverrides
```

默认值：`closeTimestampMinutes = 10`（最小 0），`fuzzyMatchFloor = 0.3`（钳制到 0.0~1.0）。

前三组 `Vec<String>` 字段（`annotationAuthors` / `revisionAuthors` / `xmpCreators`）来自隐藏痕迹提取，为「同一主体」提供补充证据。

### 输出模型

```mermaid
classDiagram
    class CompareResult {
        +u32 schemaVersion
        +String dimension
        +String disclaimer
        +Vec~CompanySnapshot~ companies
        +Vec~AlignedGroup~ groups
        +Vec~UnmatchedFile~ unmatched
        +Vec~RiskFinding~ findings
        +Vec~CompanyPairRisk~ pairRisks
        +CompareStats stats
        +Vec~FileMetaSnapshot~ files
    }

    class CompanySnapshot {
        +String companyId
        +String companyName
        +u32 fileCount
    }

    class AlignedGroup {
        +String groupId
        +String label
        +Vec~GroupCell~ cells
        +Vec~FieldDiff~ diffs
        +Option~RiskLevel~ riskLevel
        +u32 diffCount
        +u32 missingCount
        +f64 matchConfidence
        +MatchSource matchSource
    }

    class GroupCell {
        +String companyId
        +Option~String~ docId
        +Option~String~ fileName
    }

    class FieldDiff {
        +String fieldKey
        +String fieldLabel
        +FieldTier tier
        +DiffState state
        +Vec~FieldValue~ values
    }

    class FieldValue {
        +String companyId
        +Option~String~ docId
        +Option~String~ raw
        +Option~String~ normalized
        +bool isEmpty
    }

    class UnmatchedFile {
        +String docId
        +String companyId
        +String fileName
        +String reason
    }

    class CompareStats {
        +u32 companyCount
        +u32 fileCount
        +u32 groupCount
        +u32 unmatchedCount
        +u32 highCount
        +u32 mediumCount
        +u32 lowCount
        +u32 diffFieldCount
    }

    class FileMetaSnapshot {
        +String docId
        +String companyId
        +String companyName
        +String fileName
        +String creator
        +String lastModifiedBy
        +String appCompany
        +String manager
        +String application
        +bool hasHiddenMarkers
    }

    CompareResult "1" *-- "0..*" CompanySnapshot
    CompareResult "1" *-- "0..*" AlignedGroup
    CompareResult "1" *-- "0..*" UnmatchedFile
    CompareResult "1" *-- "1" CompareStats
    CompareResult "1" *-- "0..*" FileMetaSnapshot
    AlignedGroup "1" *-- "1..*" GroupCell
    AlignedGroup "1" *-- "0..*" FieldDiff
    FieldDiff "1" *-- "1..*" FieldValue
```

`dimension` 固定为 `"cross-company"`。`disclaimer` 承载合规声明，随结果一并下发，确保导出的报告始终带有「线索非结论」的说明。

`matchConfidence` 保留 3 位小数，`pairScore` 保留 2 位小数，均在 Rust 侧完成舍入，避免前后端浮点呈现不一致。

### 风险线索模型

```mermaid
classDiagram
    class RiskFinding {
        +String findingId
        +String ruleId
        +RiskLevel level
        +EvidenceKind evidenceKind
        +f64 weight
        +String problem
        +Option~String~ groupId
        +Vec~FindingRef~ refs
        +Vec~String~ fieldKeys
        +String value
        +Option~String~ entityKey
        +f64 score
        +String matchTag
        +String explain
    }

    class FindingRef {
        +String companyId
        +String docId
        +String fieldKey
        +String raw
    }

    class CompanyPairRisk {
        +String pairId
        +String companyA
        +String companyB
        +RiskLevel level
        +f64 pairScore
        +Vec~String~ ruleIds
        +Vec~String~ findingIds
        +String summary
    }

    RiskFinding "1" *-- "1..*" FindingRef
    CompanyPairRisk "1" ..> "1..*" RiskFinding : findingIds
```

`findingId` 形如 `f0001`，`pairId` 形如 `{companyA}__{companyB}`，`groupId` 形如 `g0001`。

`explain` 为人类可读的判定依据，`matchTag` 标记匹配方式，二者共同支撑「可复核」——审核人能看到线索的来源而非黑箱结论。

### 枚举

```mermaid
classDiagram
    class RiskLevel {
        <<enumeration>>
        low
        medium
        high
    }

    class DiffState {
        <<enumeration>>
        identical
        similar
        conflict
        missing
        ignored
    }

    class FieldTier {
        <<enumeration>>
        risk
        display
        ignored
    }

    class MatchSource {
        <<enumeration>>
        exact
        normalized
        fuzzy
        manual
    }

    class EvidenceKind {
        <<enumeration>>
        sameEntity
        sameOrigin
    }
```

`RiskLevel` 实现了 `Ord`，序为 `low < medium < high`，因此组级风险可直接取成员线索的最大值。

`FieldTier` 决定字段参与度：`risk` 参与规则判定，`display` 仅呈现差异，`ignored` 完全跳过。

`EvidenceKind` 区分两类证据语义：
- `sameEntity` — 指向同一个人 / 同一主体（作者、管理者、单位、批注者）
- `sameOrigin` — 指向同一来源 / 同一制作环境（模板、时间邻近、软件指纹）

## 领域关系总览

```mermaid
erDiagram
    COMPANY ||--o{ FILE : "提交"
    FILE ||--|| METADATA : "携带"
    FILE ||--o{ HIDDEN_TRACE : "残留"
    FILE }o--o| ALIGNED_GROUP : "对齐到"
    ALIGNED_GROUP ||--o{ FIELD_DIFF : "产生"
    RULE ||--o{ RISK_FINDING : "命中生成"
    RISK_FINDING }o--|| ALIGNED_GROUP : "归属"
    RISK_FINDING }o--o{ COMPANY_PAIR : "聚合为"
    COMPANY ||--o{ COMPANY_PAIR : "参与"

    COMPANY {
        string companyId PK
        string companyName
        u32 fileCount
    }
    FILE {
        string docId PK
        string companyId FK
        string fileName
        bool hasHiddenMarkers
    }
    METADATA {
        string creator
        string lastModifiedBy
        string appCompany
        string manager
        string template
    }
    HIDDEN_TRACE {
        list annotationAuthors
        list revisionAuthors
        list xmpCreators
    }
    ALIGNED_GROUP {
        string groupId PK
        string label
        f64 matchConfidence
        enum matchSource
    }
    RISK_FINDING {
        string findingId PK
        string ruleId FK
        enum level
        f64 weight
        f64 score
    }
    COMPANY_PAIR {
        string pairId PK
        enum level
        f64 pairScore
    }
```

## 契约约束

1. **命名对齐** — Rust `snake_case` ↔ TS `camelCase`，由 serde 自动转换，禁止手写映射层。
2. **版本化** — `schemaVersion` 当前为 `1`，结构破坏性变更须递增。
3. **可选性显式** — Rust `Option<T>` ↔ TS `T | null`；`skip_serializing_if` 的字段（如 `entityKey`）在 TS 侧为可选属性。
4. **枚举小写** — 所有枚举以 `lowercase` 或 `camelCase` 序列化为字符串字面量，TS 侧用联合类型 + `as const` 而非 `enum`。
5. **数值精度** — 浮点在 Rust 侧完成舍入后再序列化。

## 相关文档

- [03-比对引擎](./03-比对引擎.md) — 这些模型如何被生产出来
- [05-IPC契约](./05-IPC契约.md) — 模型在哪些命令中流转
